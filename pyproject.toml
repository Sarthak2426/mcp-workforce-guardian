[project]
name = "mcp-workforce-guardian"
version = "0.1.0"
description = "MCP server that puts a human approval step in front of destructive and anomalous workforce operations"
readme = "README.md"
authors = [
    { name = "sa2426", email = "sarthak.jindal2426@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=4.0.1",
    "matplotlib>=3.11.1",
]

[tool.uv]
package = false
